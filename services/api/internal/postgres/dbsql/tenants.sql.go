// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tenants.sql

package dbsql

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getTenant = `-- name: GetTenant :one
SELECT
  id,
  slug,
  name,
  status,
  timezone,
  locale,
  created_at,
  updated_at
FROM tenants
WHERE id = $1::uuid
`

type GetTenantParams struct {
	ID pgtype.UUID `db:"id" json:"id"`
}

type GetTenantRow struct {
	ID        pgtype.UUID        `db:"id" json:"id"`
	Slug      string             `db:"slug" json:"slug"`
	Name      string             `db:"name" json:"name"`
	Status    TenantStatus       `db:"status" json:"status"`
	Timezone  string             `db:"timezone" json:"timezone"`
	Locale    string             `db:"locale" json:"locale"`
	CreatedAt pgtype.Timestamptz `db:"created_at" json:"created_at"`
	UpdatedAt pgtype.Timestamptz `db:"updated_at" json:"updated_at"`
}

func (q *Queries) GetTenant(ctx context.Context, arg GetTenantParams) (*GetTenantRow, error) {
	row := q.db.QueryRow(ctx, getTenant, arg.ID)
	var i GetTenantRow
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.Status,
		&i.Timezone,
		&i.Locale,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return &i, err
}

const listTenants = `-- name: ListTenants :many
SELECT
  id,
  slug,
  name,
  status,
  timezone,
  locale,
  created_at,
  updated_at
FROM tenants
WHERE $1::uuid IS NULL OR id > $1::uuid
ORDER BY id ASC
LIMIT $2
`

type ListTenantsParams struct {
	AfterID  pgtype.UUID `db:"after_id" json:"after_id"`
	PageSize int32       `db:"page_size" json:"page_size"`
}

type ListTenantsRow struct {
	ID        pgtype.UUID        `db:"id" json:"id"`
	Slug      string             `db:"slug" json:"slug"`
	Name      string             `db:"name" json:"name"`
	Status    TenantStatus       `db:"status" json:"status"`
	Timezone  string             `db:"timezone" json:"timezone"`
	Locale    string             `db:"locale" json:"locale"`
	CreatedAt pgtype.Timestamptz `db:"created_at" json:"created_at"`
	UpdatedAt pgtype.Timestamptz `db:"updated_at" json:"updated_at"`
}

func (q *Queries) ListTenants(ctx context.Context, arg ListTenantsParams) ([]*ListTenantsRow, error) {
	rows, err := q.db.Query(ctx, listTenants, arg.AfterID, arg.PageSize)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []*ListTenantsRow
	for rows.Next() {
		var i ListTenantsRow
		if err := rows.Scan(
			&i.ID,
			&i.Slug,
			&i.Name,
			&i.Status,
			&i.Timezone,
			&i.Locale,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
